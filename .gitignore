# APIトークン・秘密情報は絶対にコミットしない
.env
.env.*
*.token
*.secret
secrets.*
config.local.*
credentials*.json
service-account*.json
*.pem
*.key
scripts/rewrite.env

# OS / editor
.DS_Store
Thumbs.db
*.log
node_modules/
package.json
package-lock.json

out/*.txt
out/*.html
# out/articles/ と out/rewritten/ はgit管理（Claude Codeとの受け渡し用）

# Obsidianのローカル設定（同期不要）
.obsidian/
